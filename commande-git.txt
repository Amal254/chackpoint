commande 1) mkdir learn_git

commande 2) cd learn_git

commande 3) cd >third.txt

commande 4) git init

commande 5) git add third.txt

commande 6) git commit -m "adding third.txt"
            git config --global user.email "amalbenammar8@gmail.com"
            git config --global user.name "Amal254"

commande 7) git log

commande 8) cd >fourth.txt

commande 9) git add fourth.txt

commande 10) git commit -m "adding fourth.txt"

commande 11) git rm third.txt

commande 12) git add .

commande 13) git commit -m "removing third.txt"

commande 14) git log

commande 15) git config --global core.pager 'cat'

commande 16) git config

commande 17) git config --global